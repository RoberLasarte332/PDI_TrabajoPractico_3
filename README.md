# Trabajo Practico Nº3 de Procesamiento de Imagenes
Detección de 5 dados en videos

Para crear el entorno virtual con las dependencias necesarias corra estos comandos en la terminal estando en la carpeta contenedora del trabajo:
  `python -m venv .venv`
  `.\.venv\Scripts\Activate.ps1`
  `pip install -r requirements.txt`